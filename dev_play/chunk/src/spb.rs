use crate::proto_gen;

use proto_gen::storage;
use proto_gen::storage::*;
use proto_gen::storage::{
    client_to_chunk_client::ClientToChunkClient,
    client_to_chunk_server::{
        ClientToChunk,
        ClientToChunkServer
    }
};