// DO NOT MODIFY AUTO-GENERATED BY PB-WALKER
use mysql_async::prelude::*;
use mysql_async::{FromRowError, OptsBuilder, Params, Row, Pool};
use mysql_common::row::ColumnIndex;

use mysql_common::value::Value;
use crate::mysql_shared::*;

#[derive(Default, Clone, PartialEq, Eq, Debug)]
pub struct ChannelMsgMedia  { // channel_msg_media
    pub channel_cid: u64,
    pub media_type_id: u32,
    pub msg_gid: u64,
}

impl FromRow for ChannelMsgMedia {
    fn from_row_opt(row: Row) -> Result<Self, FromRowError>
    where
        Self: Sized,
    {
        Ok(ChannelMsgMedia  {
            channel_cid: row.get(0).unwrap_or_default(),
            media_type_id: row.get(1).unwrap_or_default(),
            msg_gid: row.get(2).unwrap_or_default(),
        })
    }
}

impl ChannelMsgMedia {
    pub async fn insert(&self, spool: &SPool) -> Result<ChannelMsgMedia,MyError> {
        let mut conn = spool.pool.get_conn().await?;

        let query = format!(r"INSERT INTO {:}.channel_msg_media (channel_cid, media_type_id, msg_gid) VALUES (?, ?, ?)",&spool.database);
        let p = Params::Positional(vec![self.channel_cid.clone().into(), self.media_type_id.clone().into(), self.msg_gid.clone().into()]);

        conn.exec_iter(
            query, p
        ).await?;

        let cp = self.clone();
        Ok(cp)
    }

    // [[ for template: this code is copy of insert with 'insert' changed to 'replace' ]]
    pub async fn replace(&self, spool: &SPool) -> Result<ChannelMsgMedia,MyError> {
        let mut conn = spool.pool.get_conn().await?;

        let query = format!(r"REPLACE INTO {:}.channel_msg_media (channel_cid, media_type_id, msg_gid) VALUES (?, ?, ?)",&spool.database);
        let p = Params::Positional(vec![self.channel_cid.clone().into(), self.media_type_id.clone().into(), self.msg_gid.clone().into()]);

        conn.exec_iter(
            query, p
        ).await?;

        let cp = self.clone();
        Ok(cp)
    }

    pub async fn update(&self, spool: &SPool) -> Result<(),MyError> {
        let mut conn = spool.pool.get_conn().await?;
        let query = format!(r"UPDATE `{:}`.channel_msg_media` SET  WHERE channel_cid = ? AND media_type_id = ? AND msg_gid = ? ", &spool.database);
        let p = Params::Positional(vec![ self.channel_cid.clone().into(), self.media_type_id.clone().into(), self.msg_gid.clone().into() ]);

        let qr = conn.exec_iter(
            query, p
        ).await?;

        Ok(())
    }

    pub async fn delete(&self, spool: &SPool) -> Result<(),MyError> {
        let mut conn = spool.pool.get_conn().await?;

        let query = format!(r"DELETE FROM `{:}`.channel_msg_media` WHERE channel_cid = ? AND media_type_id = ? AND msg_gid = ? ", &spool.database);
        let p = Params::Positional(vec![self.channel_cid.clone().into(), self.media_type_id.clone().into(), self.msg_gid.clone().into()]);

        conn.exec_drop(
            query, p
        ).await?;

        Ok(())
    }
}


#[derive(Default, Debug)]
pub struct ChannelMsgMediaSelector {
    q: TQuery
}

impl ChannelMsgMediaSelector {
    pub fn new() -> Self {
        ChannelMsgMediaSelector{
            q: TQuery{
                table: "channel_msg_media",
                ..Default::default()
            }
        }
    }

    pub fn limit(&mut self, size: u32) -> &mut Self {
        self.q.limit = size;
        self
    }

    pub fn offset(&mut self, size: u32) -> &mut Self {
        self.q.offset = size;
        self
    }

    pub fn select_all(&mut self) -> &mut Self {
        // Default is select *
        self
    }

    //each column select
    pub fn select_channel_cid(&mut self) -> &mut Self {
        self.q.select_cols.push("channel_cid");
        self
    }
    
    pub fn select_media_type_id(&mut self) -> &mut Self {
        self.q.select_cols.push("media_type_id");
        self
    }
    
    pub fn select_msg_gid(&mut self) -> &mut Self {
        self.q.select_cols.push("msg_gid");
        self
    }
    

    pub async fn _get_rows_with_size(&mut self, session: &SPool, size: i64) -> Result<Vec<ChannelMsgMedia>, MyError>   {
        let mut conn = session.pool.get_conn().await?;
        let(cql_query, query_values) = self.q._to_sql_selector(&session.database);

        println!("{} - {:?}", &cql_query, &query_values);

        let p = Params::Positional(query_values);

        let query_result = conn
            .exec_map(
                cql_query,p,
                |obj: ChannelMsgMedia| obj
            ).await?;

        Ok(query_result)
    }

    pub async fn get_rows(&mut self, session: &SPool) -> Result<Vec<ChannelMsgMedia>, MyError>{
        self._get_rows_with_size(session,-1).await
    }

    pub async fn get_row(&mut self, session: &SPool) -> Result<ChannelMsgMedia, MyError>{
        let rows = self._get_rows_with_size(session,1).await?;

        let opt = rows.get(0);
        match opt {
            Some(row) => Ok(row.to_owned()),
            None => Err(MyError::NotFound)
        }
    }

    // Modifiers
    
    pub fn order_by_channel_cid_asc(&mut self) -> &mut Self {
		self.q.order_by.push("channel_cid ASC");
        self
    }

	pub fn order_by_channel_cid_desc(&mut self) -> &mut Self {
		self.q.order_by.push("channel_cid DESC");
        self
    }

    pub fn order_by_media_type_id_asc(&mut self) -> &mut Self {
		self.q.order_by.push("media_type_id ASC");
        self
    }

	pub fn order_by_media_type_id_desc(&mut self) -> &mut Self {
		self.q.order_by.push("media_type_id DESC");
        self
    }

    pub fn order_by_msg_gid_asc(&mut self) -> &mut Self {
		self.q.order_by.push("msg_gid ASC");
        self
    }

	pub fn order_by_msg_gid_desc(&mut self) -> &mut Self {
		self.q.order_by.push("msg_gid DESC");
        self
    }

    
    pub fn channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    
    pub fn channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

}

// Updater

#[derive(Default, Debug)]
pub struct ChannelMsgMediaUpdater {
    q: TQuery
}

impl ChannelMsgMediaUpdater {
    pub fn new() -> Self {
        ChannelMsgMediaUpdater{
            q: TQuery{
                table: "channel_msg_media",
                ..Default::default()
            }
        }
    }

    pub fn limit(&mut self, size: u32) -> &mut Self {
        self.q.limit = size;
        self
    }

    //each column delete
    pub fn set_channel_cid(&mut self, val :u64) -> &mut Self {
        self.q.updates.insert("channel_cid",val.into());
        self
    }
    
    pub fn set_media_type_id(&mut self, val :u32) -> &mut Self {
        self.q.updates.insert("media_type_id",val.into());
        self
    }
    
    pub fn set_msg_gid(&mut self, val :u64) -> &mut Self {
        self.q.updates.insert("msg_gid",val.into());
        self
    }
    

    pub async fn update(&mut self, session: &SPool) -> Result<(),MyError> {
        update_rows(&self.q, session).await
    }

    
    pub fn order_by_channel_cid_asc(&mut self) -> &mut Self {
		self.q.order_by.push("channel_cid ASC");
        self
    }

	pub fn order_by_channel_cid_desc(&mut self) -> &mut Self {
		self.q.order_by.push("channel_cid DESC");
        self
    }

    pub fn order_by_media_type_id_asc(&mut self) -> &mut Self {
		self.q.order_by.push("media_type_id ASC");
        self
    }

	pub fn order_by_media_type_id_desc(&mut self) -> &mut Self {
		self.q.order_by.push("media_type_id DESC");
        self
    }

    pub fn order_by_msg_gid_asc(&mut self) -> &mut Self {
		self.q.order_by.push("msg_gid ASC");
        self
    }

	pub fn order_by_msg_gid_desc(&mut self) -> &mut Self {
		self.q.order_by.push("msg_gid DESC");
        self
    }

    
    pub fn channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    
    pub fn channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

}

// Deleter

#[derive(Default, Debug)]
pub struct ChannelMsgMediaDeleter {
    q: TQuery
}

impl ChannelMsgMediaDeleter {
    pub fn new() -> Self {
        ChannelMsgMediaDeleter{
            q: TQuery{
                table: "channel_msg_media",
                ..Default::default()
            }
        }
    }

    pub fn limit(&mut self, size: u32) -> &mut Self {
        self.q.limit = size;
        self
    }

    pub async fn delete(&mut self, session: &SPool) -> Result<(),MyError> {
        delete_rows(&self.q, session).await
    }

    
    pub fn order_by_channel_cid_asc(&mut self) -> &mut Self {
		self.q.order_by.push("channel_cid ASC");
        self
    }

	pub fn order_by_channel_cid_desc(&mut self) -> &mut Self {
		self.q.order_by.push("channel_cid DESC");
        self
    }

    pub fn order_by_media_type_id_asc(&mut self) -> &mut Self {
		self.q.order_by.push("media_type_id ASC");
        self
    }

	pub fn order_by_media_type_id_desc(&mut self) -> &mut Self {
		self.q.order_by.push("media_type_id DESC");
        self
    }

    pub fn order_by_msg_gid_asc(&mut self) -> &mut Self {
		self.q.order_by.push("msg_gid ASC");
        self
    }

	pub fn order_by_msg_gid_desc(&mut self) -> &mut Self {
		self.q.order_by.push("msg_gid DESC");
        self
    }

    
    pub fn channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_channel_cid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR channel_cid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: " media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_eq (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_lt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_le (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_gt (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_media_type_id_ge (&mut self, val: u32 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR media_type_id >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: " msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn and_msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "AND msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_eq (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid = ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_lt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid < ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_le (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid <= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_gt (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid > ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    pub fn or_msg_gid_ge (&mut self, val: u64 ) -> &mut Self {
        let w = WhereClause{
            condition: "OR msg_gid >= ?".to_string(),
            args: val.into(),
        };
        self.q.wheres.push(w);
        self
    }

    
    pub fn channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_channel_cid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR channel_cid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_media_type_id_in (&mut self, val: Vec<u32> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR media_type_id IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!(" msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn and_msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("AND msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

    pub fn or_msg_gid_in (&mut self, val: Vec<u64> ) -> &mut Self {
		let len = val.len();
        if len == 0 {
            return self
        }

        let mut marks = "?,".repeat(len);
        marks.remove(marks.len()-1);

		let arr = val.iter().map(|v|v.into()).collect();

        let w = WhereInClause{
			condition: format!("OR msg_gid IN ({})", marks),
            args: arr,
        };
        self.q.wheres_ins.push(w);
        self
    }

}

pub async fn channel_msg_media_mass_insert(arr :&Vec<ChannelMsgMedia>, spool: &SPool) -> Result<(),MyError> {
    let mut conn = spool.pool.get_conn().await?;
    if arr.len() == 0 {
        return Err(MyError::EmptySql)
    }
     let mut insert_fields = "(channel_cid, media_type_id, msg_gid)";

     let mut vals_str = "(?, ?, ?), ".repeat(arr.len());
    let vals_str = &vals_str[0..(vals_str.len()-2)];

    let query = format!(r"INSERT INTO {:}.tweet {:} VALUES {:}", &spool.database, insert_fields, vals_str);

    let mut arr_vals = vec![];
    for ar in arr {
        arr_vals.push(ar.channel_cid.clone().into());
        arr_vals.push(ar.media_type_id.clone().into());
        arr_vals.push(ar.msg_gid.clone().into());}

    let p = Params::Positional(arr_vals);

    println!("{} - {:?}", &query, &p);

    let qr = conn.exec_iter(
        query, p
    ).await?;

    Ok(())
}

// Index
pub async fn get_channel_msg_media(channel_cid: u64,media_type_id: u32,msg_gid: u64, spool: &SPool) -> Result<ChannelMsgMedia,MyError> {
	let m = ChannelMsgMediaSelector::new()
		.channel_cid_eq(channel_cid)
		.and_media_type_id_eq(media_type_id)
		.and_msg_gid_eq(msg_gid)
		.get_row(spool).await?;
	Ok(m)
}
